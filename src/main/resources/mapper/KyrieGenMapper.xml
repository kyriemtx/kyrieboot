<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mtx.kyrieboot.kyriegen.mapper.KyrieGenMapper">

    <resultMap type="com.mtx.kyrieboot.kyriegen.entity.TableEntity" id="TableMap">
        <result property="tableName" column="table_name" jdbcType="VARCHAR"/>
        <result property="tableComment" column="table_comment" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="VARCHAR"/>
        <result property="updateTime" column="update_time" jdbcType="VARCHAR"/>
    </resultMap>

    <select id="kyrieQueryTableList" resultMap="TableMap">
        select table_name tableName, table_comment tableComment,
        create_time createTime,update_time updateTime from information_schema.tables
        where table_schema = (select database()) and table_name NOT REGEXP
        'kyrie_'
    </select>

    <select id="selectByTableName" parameterType="java.lang.String" resultType="com.mtx.kyrieboot.kyriegen.entity.TableEntity">
        select table_name tableName, table_comment comments,
        create_time createTime,update_time updateTime from information_schema.tables
        where table_schema = (select database()) and TABLE_NAME = #{tableName}
    </select>

    <select id="queryDatabaseName" resultType="String">
	select database()
	</select>

    <select id="queryReferenced" resultType="com.mtx.kyrieboot.kyriegen.entity.ReferencedTable">
        select table_name tableName,column_name columnName,REFERENCED_TABLE_NAME referencedTableName,REFERENCED_column_name
        referencedColumnName from INFORMATION_SCHEMA.KEY_COLUMN_USAGE where table_schema = (select
        database()) and table_name = #{tableName} and constraint_name != 'PRIMARY'
    </select>

    <select id="queryColumns" resultType="map">
        select column_name
        columnName, IS_NULLABLE isNullable,COLUMN_DEFAULT columnDefault,character_maximum_length dataLength,data_type dataType, column_comment
        columnComment, column_key columnKey, extra from
        information_schema.columns
        where table_name = #{tableName} and table_schema = (select database())
        order by ordinal_position
    </select>

    <select id="selectAllColumns" resultType="com.mtx.kyrieboot.kyriegen.entity.ColumnEntity">
        select column_name
        columnName, IS_NULLABLE isNullable,COLUMN_DEFAULT columnDefault,character_maximum_length dataLength,data_type dataType, column_comment
        comments, (case  when column_key = 'PRI' THEN 'P' END)
        AS columnKey, extra from
        information_schema.columns
        where table_name = #{tableName} and table_schema = (select database())
        order by ordinal_position
    </select>

</mapper>